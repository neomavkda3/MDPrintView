import Testing
@testable import mdview

@Suite("Placeholder")
struct PlaceholderTests {
    @Test("placeholder so the test target compiles")
    func placeholder() {
        #expect(1 + 1 == 2)
    }
}
